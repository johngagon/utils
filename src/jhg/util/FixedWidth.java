package jhg.util;

public class FixedWidth {

	/*
	 * Fixed width:
	 * 
	 * Be able to declare a fixed width spec.
	 * 
	 * Check a list of strings or string arr against a fixed width spec.
	 *  
	 * 
	 * a. The list size should be shorter
	 * b. Be able to check if fields in the order given should fit in the width.
	 * c. Be able to stuff the list into the fixed width line with the option of truncating.
	 * d. By default left align, pass in int list of anything to right align.
	 * 
	 * Produce the fixed width row for output or for file.
	 * 
	 * Be able to read and parse fixed width according to the Fixed width's definition as an object. Produce String[] or list of strings.
	 *  
	 * 
	 */
}
